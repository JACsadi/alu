#include <stdio.h>
#include <stdarg.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>
#define ll long long
#define ull unsigned long long
#define f(i, n) for (int i = 0; i < n; i++)
#define ma(k, n) (k*) malloc(n*sizeof(k));
#define ima(y, k, n) k* y = (k*) malloc(n*sizeof(k));
#define sd(i) scanf("%d", &i)
int main() {
   int t;sd(t);
   while(t--) {
    char str[100001];
    scanf(" %s", str);
    int siz = strlen(str);
    int* arr = (int*) calloc(siz, sizeof(int));
    f(i,n) {
        if()
    }
   }
     return 0;
}