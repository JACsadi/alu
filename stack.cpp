#include <iostream>
#include <stack>
#include <queue>
#include <deque>
using namespace std; 
int main() {
    vector<int> alu;
    alu.
    // stack<int> facto;
    // facto.push(1);
    // facto.push(1);  
    // int n;
    // scanf("%d",&n);
    // while(1)
    // {
    //    int factorial=facto.top();
    //    printf("%d\n",factorial);
    //    facto.pop();// ebar top er ta delete korbe
    //   int curr_num= facto.top(); //top mane uporer ta uthano
    //   if(curr_num == n) break;
    //     curr_num+=1; // next number ta paoar jonne, suppose 1 chilo 2 hobe
    //    facto.pop();
    //    factorial*=curr_num;
    //    facto.push(curr_num);
    //    facto.push(factorial);// 
    // }
//     queue<int> line;
//     for(int i = 0; i < 10; i++) line.push(i);
//    for(int i = 0; i < 13; i++) {
//      printf("%d %d\n ", line.front(), line.size());
//      line.pop();
// push_front pusj_bac
 deque<int> line;
for(int i = 0; i < 10; i++) {
    line.push_back(i);
    line.
   
}
   // .push() to push element at the end
//    .pop() to del first element
// .front() to return the first element
//.size() to return size
//.empty() to check if empty or not 



 