#include <bits/stdc++.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
using namespace std;
int main(void) {
    int n;
    char c;
    scanf("%d", &n);
    getchar(); 
    getchar();
    while (n--) {
        char stack[100];
        int j = 0;
        while(1) {
        string temp;
        getline(cin,temp);
        if(temp.length() < 1) break;
        else {
         printf("%s", temp);    
                }
            }
      
    }

    return 0;
}